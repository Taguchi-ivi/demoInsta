-# = render 'commons/back'
- if !current_page?(root_path)
    .back-btn
        -# = link_to :back do
        = link_to root_path do
            -# = image_tag 'back.png'
            %i.fa-solid.fa-arrow-left-long