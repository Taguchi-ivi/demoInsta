-# Comments INDEX 
.container
    %h1.page-header Comment
    = render 'commons/back'
    -# %ul
    -#     - @comment.errors.full_messages.each do |message|
    -#         %li= message
    -# .comment#comment-index{ data: { article_id:  @article.id, user_id: current_user.id } }
    .comment#comment-index{ data: { article_id:  @article.id } }
        -# - @comment.each do |comment|
        -#     .comment-item
        -#         .comment-item-wrapper
        -#             .comment-item-left
        -#                 = image_tag comment.article.user.avatar_image(comment.article.user)
        -#             .comment-item-right
        -#                 .comment-item-right-name
        -#                     = comment.article.user.account
        -#                 .comment-item-right-content
        -#                     = comment.content
    .comment-form
        .comment-form-box
            .comment-form-box-left
                = image_tag 'comment-mark.png'
            .comment-form-box-right
                = text_field :comment_content, '', class: 'comment-content'
            -# .comment-form-box-right2
                -# = f.submit '保存', class: 'btn btn-primary'

=  javascript_pack_tag 'comment'